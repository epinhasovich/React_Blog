import React from 'React';


const TextBox = () => {
    return (
        <div>
            <input type='text' onChange={event => this.handleNewTweet(event)} />
            <button onClick={() => addTweet(tweet)}>Tweet</button>
        </div>
    )
}

expor
